function solve(studentName, studentAge, studentGrade) {
  console.log(
    `Name: ${studentName}, Age: ${studentAge}, Grade: ${studentGrade.toFixed(
      2
    )}`
  );
}

solve("John", 15, 5.54678);
